@using Microsoft.Extensions.Options
@*@inject IOptions<EShop.Domain.StripeSettings> Stripe*@
@model ISH.Service.Dtos.Cart.CartDto

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@*
<div class="container">


    <div class="row m-4">
        @if (Model.CartPrice != 0)
        {
            <form asp-controller="ShoppingCard" asp-action="PayOrder" method="post">
                <article>
                    <script src="https://checkout.stripe.com/checkout.js"
                            class="stripe-button"
                            data-key="@Stripe.Value.PublishableKey"
                            data-locale="auto"
                            data-description="EShop Application Payment"
                            data-amount="@(Model.CartPrice * 100)"
                            data-label="Pay $@Model.CartPrice">
                    </script>
                </article>
            </form>
        }
    </div>

    <div class="row m-4">
        <table class="table">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Product Name</th>
                    <th scope="col">Quantity</th>
                    <th scope="col">ProductPrice</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Tickets.Count == 0)
                {
                    <tr>
                        <td colspan="5">No active Tickets</td>
                    </tr>
                }
                else
                {
                    @for (int i = 0; i < Model.Tickets.Count; i++)
                    {
                        var item = Model.Tickets[i];

                        <tr>
                            <th scope="row">@(i + 1)</th>
                            <td>@item.CurrentTicket.ProductName</td>
                            <td>@item.Quantity</td>
                            <td>$@item.CurrentTicket.ProductPrice</td>
                            <td><a asp-controller="ShoppingCard" asp-action="DeleteFromShoppingCart" asp-route-id="@item.Guid" class="btn btn-danger">Delete From ShoppingCart</a></td>
                        </tr>
                    }
                }
            </tbody>
            <tfoot class="thead-dark">
                <tr>
                    <th scope="col">CartPrice:</th>
                    <th scope="col"></th>
                    <th scope="col"></th>
                    <th scope="col"></th>
                    <th scope="col">$@Model.CartPrice</th>
                </tr>
            </tfoot>
        </table>
    </div>

</div>*@